<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="nasaro.verses.model.mapper.ReferenceMapper">

	<!-- faq 목록 조회 -->
	<select id="referenceList">
		SELECT r.MEMBER_NO, 
			m.MEMBER_NAME, 
			r.REFERENCE_NO, 
			r.REFERENCE_TITLE, 
			r.REFERENCE_CONTENT, 
			DATE_FORMAT(r.REFERENCE_DATE, '%m-%d') REFERENCE_DATE 
		FROM `REFERENCE` r join `member` m on r.MEMBER_NO =m.MEMBER_NO  
		ORDER BY r.REFERENCE_NO desc;
	
	</select>
	
	<select id="detailedReference">
		SELECT * FROM `REFERENCE` WHERE reference_no = #{No};
	</select>
	

</mapper>

